# ---- Core app runtime (needed to run the Streamlit demo) ----
streamlit>=1.33,<2.0
requests>=2.31
python-dotenv>=1.0
scikit-learn>=1.3        # TF-IDF retrieval for dataset-grounded few-shot
pandas>=2.0

# ---- Dataset ingestion (used by scripts/prepare_data.py) ----
# These pull the REAL public datasets listed in the problem statement.
# The app still runs without them (it falls back to the bundled real samples),
# but run `python scripts/prepare_data.py` once to build the full corpus.
datasets>=2.18
huggingface_hub>=0.23

# ---- Optional, improves Indic syllable/rhyme analysis if present ----
indic-transliteration>=2.3 ; python_version >= "3.8"
